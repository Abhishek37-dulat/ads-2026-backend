package com.relay.shared;

/** Ad platforms Relay can fan out to. New platforms are additive (adapters package only). */
public enum Platform {
    META,
    GOOGLE,
    TIKTOK,
    LINKEDIN,
    PINTEREST,
    SNAP,
    REDDIT
}
