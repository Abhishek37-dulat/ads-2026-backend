package com.relay.shared;

/** Relay's canonical campaign objectives — each adapter maps these to a native objective. */
public enum Objective {
    AWARENESS,
    TRAFFIC,
    ENGAGEMENT,
    LEADS,
    APP_PROMOTION,
    SALES,
    CALLS
}
